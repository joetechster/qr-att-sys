# Face-recognition stack for Windows. Install with --no-deps so pip does not
# try to compile dlib from source:
#     pip install --no-deps -r requirements-face.txt
dlib-bin>=20.0
face_recognition_models>=0.3
Click>=6.0
colorama>=0.4
face_recognition>=1.3
# face_recognition_models 0.3.0 imports pkg_resources from setuptools.
# setuptools >=81 removed pkg_resources, so we pin below that.
setuptools<81
